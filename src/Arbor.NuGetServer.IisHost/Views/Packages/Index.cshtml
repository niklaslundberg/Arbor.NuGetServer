@using Arbor.NuGetServer.Core.Extensions
@using Arbor.NuGetServer.IisHost.Models
@model Arbor.NuGetServer.IisHost.Models.PackagesViewModel

<h2>Packages</h2>

<h3>NuGet packages (@Model.TotalNuGetSize.AsSuitableByteUnit())</h3>

@if (Model.RelativeNuGetPaths.Any())
{
    <h4>Top packages by size</h4>

    <ul>
        @foreach (CustomFileInfo item in Model.TopPackagesBySize)
        {
            <li>@item.RelativePath, <strong>@item.FileInfo.Length.AsSuitableByteUnit()</strong></li>
        }
    </ul>

    <ul>
        @foreach (CustomFileInfo item in Model.RelativeNuGetPaths)
        {
            <li>@item.RelativePath, <strong>@item.FileInfo.Length.AsSuitableByteUnit()</strong></li>
        }
    </ul>
}

<h3>Other files (@Model.TotalOtherFileSize.AsSuitableByteUnit())</h3>

@if (Model.RelativeOtherPaths.Any())
{
    <ul>
        @foreach (CustomFileInfo item in Model.RelativeOtherPaths)
        {
            <li>
                @item.RelativePath <strong>@(item.FileInfo.Length.AsSuitableByteUnit())</strong>
            </li>
        }
    </ul>
}
